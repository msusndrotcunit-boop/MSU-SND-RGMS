# Production Environment Configuration
# This file is used when building the React app for production (npm run build)

# Django Backend API URL (same domain - monorepo deployment)
VITE_API_URL=https://msu-snd-rgms-1.onrender.com

# WebSocket URL for real-time updates (same domain)
VITE_WS_URL=wss://msu-snd-rgms-1.onrender.com

# Environment
VITE_ENV=production
