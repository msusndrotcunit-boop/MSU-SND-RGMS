# Production Environment Configuration
# This file is used when building the React app for production (npm run build)

# Monorepo Deployment - Single URL for everything
# Django Backend API URL (Daphne ASGI handles both HTTP and WebSocket)
VITE_API_URL=https://msu-snd-rgms-1.onrender.com

# WebSocket URL for real-time updates (Same URL as API)
VITE_WS_URL=wss://msu-snd-rgms-1.onrender.com

# Environment
VITE_ENV=production
