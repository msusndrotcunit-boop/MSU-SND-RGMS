# Production Environment Configuration
# This file is used when building the React app for production (npm run build)

# Django Backend API URL (Render.com deployment)
# Update this URL after deploying the Django backend to Render.com
VITE_API_URL=https://rotc-django-web.onrender.com

# WebSocket URL for real-time updates (Django Channels on Render.com)
# Update this URL after deploying the Django Channels service to Render.com
VITE_WS_URL=wss://rotc-django-channels.onrender.com

# Environment
VITE_ENV=production
